---
const apps = [
  { name: 'mailctl',   href: '/mailctl',   color: '#00d97e' },
  { name: 'calctl',    href: '/calctl',    color: '#6366f1' },
  { name: 'taskctl',   href: '/taskctl',   color: '#f59e0b' },
  { name: 'notectl',   href: '/notectl',   color: '#a78bfa' },
  { name: 'budgetctl', href: '/budgetctl', color: '#10b981' },
  { name: 'diaryctl',  href: '/diaryctl',  color: '#fb923c' },
  { name: 'timectl',   href: '/timectl',   color: '#22d3ee' },
  { name: 'habctl',    href: '/habctl',    color: '#84cc16' },
  { name: 'postctl',   href: 'https://postctl.sh', color: '#ec4899', external: true },
];

const currentPath = Astro.url.pathname;
const isAppPage = apps.some(a => a.href === currentPath);
---

<header id="main-nav" class="main-nav">
  <nav style="max-width: 1200px; margin: 0 auto; padding: 0 1.5rem; display: flex; align-items: center; justify-content: space-between; height: 56px;">

    <!-- Logo -->
    <a href="/" style="font-family: 'JetBrains Mono', monospace; font-size: 1.1rem; font-weight: 700; color: var(--color-terminal-green); text-decoration: none; letter-spacing: -0.02em; flex-shrink: 0;">
      <span style="color: var(--color-terminal-text);">mission</span>ctl
    </a>

    <!-- Center: Home + Apps dropdown + Docs (desktop) -->
    <div style="display: flex; gap: 0.5rem; align-items: center;" class="nav-center">

      <!-- Home -->
      <a
        href="/"
        class={`nav-link${currentPath === '/' ? ' nav-link--active' : ''}`}
      >
        <span class="lang-en">home</span><span class="lang-de">start</span>
      </a>

      <!-- Apps dropdown -->
      <div class="apps-dropdown" id="apps-dropdown">
        <button
          class={`apps-trigger${isAppPage ? ' is-active' : ''}`}
          id="apps-trigger"
          aria-haspopup="true"
          aria-expanded="false"
        >
          apps
          <svg class="apps-chevron" width="10" height="10" viewBox="0 0 10 10" fill="none" aria-hidden="true">
            <path d="M2 3.5L5 6.5L8 3.5" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
        </button>

        <!-- Dropdown panel -->
        <div class="apps-panel" id="apps-panel" role="menu">
          <div class="apps-grid">
            {apps.map(app => (
              <a
                href={app.href}
                class="app-chip"
                style={`--chip-color: ${app.color};`}
                role="menuitem"
                {...(app.external ? { target: '_blank', rel: 'noopener' } : {})}
              >
                <span class="app-chip-dot"></span>
                {app.name}{app.external ? ' ↗' : ''}
              </a>
            ))}
          </div>
        </div>
      </div>

      <!-- Docs link -->
      <a
        href="/docs"
        class={`nav-link${currentPath.startsWith('/docs') ? ' nav-link--active' : ''}`}
      >
        <span class="lang-en">docs</span><span class="lang-de">doku</span>
      </a>

      <!-- Changelog link -->
      <a
        href="/changelog"
        class={`nav-link${currentPath.startsWith('/changelog') ? ' nav-link--active' : ''}`}
      >
        <span class="lang-en">changelog</span><span class="lang-de">changelog</span>
      </a>

    </div>

    <div style="display: flex; align-items: center; gap: 0.5rem;">
      <!-- Mobile menu trigger -->
      <button
        id="mobile-menu-trigger"
        class="mobile-menu-trigger"
        aria-haspopup="true"
        aria-expanded="false"
        aria-controls="mobile-menu-panel"
        aria-label="Menu"
      >
        <svg width="18" height="18" viewBox="0 0 18 18" fill="none" aria-hidden="true">
          <path class="burger-top" d="M2.5 5h13" stroke="currentColor" stroke-width="1.6" stroke-linecap="round"/>
          <path class="burger-mid" d="M2.5 9h13" stroke="currentColor" stroke-width="1.6" stroke-linecap="round"/>
          <path class="burger-bot" d="M2.5 13h13" stroke="currentColor" stroke-width="1.6" stroke-linecap="round"/>
        </svg>
      </button>

      <!-- Language toggle -->
      <button id="lang-toggle" class="nav-lang-toggle" aria-label="Switch language" type="button">
        <span class="lang-en">DE</span><span class="lang-de">EN</span>
      </button>

      <!-- Theme toggle: cycles dark → light → auto → dark -->
      <button id="theme-toggle" class="nav-theme-toggle" aria-label="Cycle theme: dark, light, or auto (follows system)" type="button">
        <svg class="theme-icon-sun" width="16" height="16" viewBox="0 0 16 16" fill="none" aria-hidden="true">
          <circle cx="8" cy="8" r="3.4" stroke="currentColor" stroke-width="1.4"/>
          <path d="M8 1v1.6M8 13.4V15M15 8h-1.6M2.6 8H1M12.7 3.3l-1.1 1.1M4.4 11.6l-1.1 1.1M12.7 12.7l-1.1-1.1M4.4 4.4L3.3 3.3" stroke="currentColor" stroke-width="1.4" stroke-linecap="round"/>
        </svg>
        <svg class="theme-icon-moon" width="16" height="16" viewBox="0 0 16 16" fill="none" aria-hidden="true">
          <path d="M13.5 9.5A6 6 0 0 1 6.5 2.5a6 6 0 1 0 7 7Z" stroke="currentColor" stroke-width="1.4" stroke-linejoin="round"/>
        </svg>
        <svg class="theme-icon-auto" width="16" height="16" viewBox="0 0 16 16" fill="none" aria-hidden="true">
          <rect x="1.5" y="2.5" width="13" height="8.5" rx="1.2" stroke="currentColor" stroke-width="1.4"/>
          <path d="M5.5 14h5M8 11v3" stroke="currentColor" stroke-width="1.4" stroke-linecap="round"/>
        </svg>
      </button>

      <!-- Buy button -->
      <a
        href="/#pricing"
        class="nav-buy"
      >
        <span class="lang-en">Buy</span><span class="lang-de">Kaufen</span>
      </a>
    </div>
  </nav>

  <!-- Mobile menu panel -->
  <div class="mobile-menu-panel" id="mobile-menu-panel" role="menu">
    <a href="/" class={`mobile-menu-link${currentPath === '/' ? ' mobile-menu-link--active' : ''}`} role="menuitem"><span class="lang-en">home</span><span class="lang-de">start</span></a>
    <a href="/docs" class={`mobile-menu-link${currentPath.startsWith('/docs') ? ' mobile-menu-link--active' : ''}`} role="menuitem"><span class="lang-en">docs</span><span class="lang-de">doku</span></a>
    <a href="/changelog" class={`mobile-menu-link${currentPath.startsWith('/changelog') ? ' mobile-menu-link--active' : ''}`} role="menuitem"><span class="lang-en">changelog</span><span class="lang-de">changelog</span></a>
    <button id="lang-toggle-mobile" class="mobile-menu-link" role="menuitem" type="button" style="background:none;border:none;text-align:left;cursor:pointer;width:100%;">
      <span class="lang-en">Deutsch</span><span class="lang-de">English</span>
    </button>
    <button id="theme-toggle-mobile" class="mobile-menu-link" role="menuitem" type="button" style="background:none;border:none;text-align:left;cursor:pointer;width:100%;">
      <span class="lang-en theme-label-to-light">Switch to light</span><span class="lang-de theme-label-to-light">Zu hell wechseln</span>
      <span class="lang-en theme-label-to-auto">Switch to auto</span><span class="lang-de theme-label-to-auto">Zu automatisch wechseln</span>
      <span class="lang-en theme-label-to-dark">Switch to dark</span><span class="lang-de theme-label-to-dark">Zu dunkel wechseln</span>
    </button>
    <p class="mobile-menu-label">apps</p>
    <div class="mobile-apps-grid">
      {apps.map(app => (
        <a
          href={app.href}
          class="app-chip"
          style={`--chip-color: ${app.color};`}
          role="menuitem"
          {...(app.external ? { target: '_blank', rel: 'noopener' } : {})}
        >
          <span class="app-chip-dot"></span>
          {app.name}{app.external ? ' ↗' : ''}
        </a>
      ))}
    </div>
  </div>
</header>

<style>
  /* ── Nav shell: floating island ─────────────────────────── */
  .main-nav {
    position: relative;
    width: calc(100% - 2rem);
    max-width: 1100px;
    margin: 0.75rem auto 0;
    border-radius: 10px;
    background: rgba(var(--color-terminal-bg-rgb), 0.78);
    border: 1px solid rgba(var(--color-terminal-border-rgb), 0.85);
    box-shadow: 0 16px 40px rgba(0,0,0,0.35), 0 0 0 1px rgba(var(--color-hover-tint), 0.02);
    backdrop-filter: blur(16px);
    -webkit-backdrop-filter: blur(16px);
    transition: background 0.2s ease, border-color 0.2s ease, box-shadow 0.2s ease;
  }
  .main-nav.scrolled {
    background: rgba(var(--color-terminal-bg-rgb), 0.92);
    border-color: rgba(var(--color-terminal-border-rgb), 0.95);
    box-shadow: 0 16px 44px rgba(0,0,0,0.45), 0 0 0 1px rgba(var(--color-terminal-green-rgb), 0.06);
  }
  /* ── Shared link style ──────────────────────────────────── */
  .nav-link {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.8rem;
    padding: 0.35rem 0.75rem;
    border-radius: 4px;
    text-decoration: none;
    color: var(--color-terminal-dim);
    transition: color 0.15s ease, background 0.15s ease;
  }
  .nav-link:hover,
  .nav-link--active {
    color: var(--color-terminal-green);
    background: rgba(var(--color-terminal-green-rgb), 0.07);
  }

  /* ── Language toggle ────────────────────────────────────── */
  .nav-lang-toggle {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.8rem;
    padding: 0.35rem 0.6rem;
    border-radius: 4px;
    background: transparent;
    border: none;
    cursor: pointer;
    color: var(--color-terminal-dim);
    transition: color 0.15s ease, background 0.15s ease;
  }
  .nav-lang-toggle:hover {
    color: var(--color-terminal-green);
    background: rgba(var(--color-terminal-green-rgb), 0.07);
  }

  /* ── Theme toggle ───────────────────────────────────────── */
  .nav-theme-toggle {
    font-family: 'JetBrains Mono', monospace;
    padding: 0.35rem 0.5rem;
    border-radius: 4px;
    background: transparent;
    border: none;
    cursor: pointer;
    color: var(--color-terminal-dim);
    display: flex;
    align-items: center;
    transition: color 0.15s ease, background 0.15s ease;
  }
  .nav-theme-toggle:hover {
    color: var(--color-terminal-green);
    background: rgba(var(--color-terminal-green-rgb), 0.07);
  }
  /* Icon shows the CURRENTLY CHOSEN theme (data-theme-pref: dark/light/
     auto — not data-theme, which is auto's resolved light-or-dark value).
     Cycle order is dark → light → auto → dark, so moon is the default/base
     state (covers both "unset" and "dark" explicitly). */
  .theme-icon-sun, .theme-icon-auto { display: none; }
  .theme-icon-moon { display: block; }
  :global([data-theme-pref="light"]) .theme-icon-sun { display: block; }
  :global([data-theme-pref="light"]) .theme-icon-moon { display: none; }
  :global([data-theme-pref="auto"]) .theme-icon-auto { display: block; }
  :global([data-theme-pref="auto"]) .theme-icon-moon { display: none; }
  /* Mobile menu's text-label equivalent: shows the NEXT destination in the
     cycle (dark→light, light→auto, auto→dark), since a menu item reads more
     naturally as an action ("switch to X") than a current-state display. */
  .theme-label-to-auto, .theme-label-to-dark { display: none; }
  :global([data-theme-pref="light"]) .theme-label-to-light { display: none; }
  :global([data-theme-pref="light"]) .theme-label-to-auto { display: inline; }
  :global([data-theme-pref="auto"]) .theme-label-to-light { display: none; }
  :global([data-theme-pref="auto"]) .theme-label-to-dark { display: inline; }

  /* ── Apps trigger button ────────────────────────────────── */
  .apps-trigger {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.8rem;
    padding: 0.35rem 0.65rem;
    border-radius: 4px;
    background: transparent;
    border: none;
    cursor: pointer;
    color: var(--color-terminal-dim);
    display: flex;
    align-items: center;
    gap: 0.35rem;
    transition: color 0.15s ease, background 0.15s ease;
  }
  .apps-trigger:hover,
  .apps-trigger.is-active,
  .apps-trigger[aria-expanded="true"] {
    color: var(--color-terminal-green);
    background: rgba(var(--color-terminal-green-rgb), 0.07);
  }
  .apps-chevron {
    transition: transform 0.2s ease;
    flex-shrink: 0;
  }
  .apps-trigger[aria-expanded="true"] .apps-chevron {
    transform: rotate(180deg);
  }

  /* ── Dropdown container ─────────────────────────────────── */
  .apps-dropdown {
    position: relative;
  }

  /* ── Panel ──────────────────────────────────────────────── */
  .apps-panel {
    position: absolute;
    top: calc(100% + 2px);
    left: 50%;
    transform: translateX(-50%);
    width: 340px;
    background: rgba(var(--color-terminal-bg-rgb),0.97);
    border: 1px solid rgba(var(--color-terminal-border-rgb),0.9);
    border-radius: 10px;
    padding: 0.75rem;
    box-shadow: 0 20px 60px rgba(0,0,0,0.6), 0 0 0 1px rgba(var(--color-hover-tint),0.03);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);

    /* invisible bridge fills the gap so mouse can travel from trigger → panel */
  /* hidden state */
    opacity: 0;
    visibility: hidden;
    transform: translateX(-50%) translateY(-4px);
    transition: opacity 0.18s ease, transform 0.18s ease, visibility 0.18s;
    z-index: 100;
  }
  .apps-panel.open {
    opacity: 1;
    visibility: visible;
    transform: translateX(-50%) translateY(0);
  }

  /* arrow pointer */
  .apps-panel::before {
    content: '';
    position: absolute;
    top: -5px;
    left: 50%;
    transform: translateX(-50%) rotate(45deg);
    width: 10px;
    height: 10px;
    background: rgba(var(--color-terminal-bg-rgb),0.97);
    border-left: 1px solid rgba(var(--color-terminal-border-rgb),0.9);
    border-top: 1px solid rgba(var(--color-terminal-border-rgb),0.9);
  }

  /* ── App grid ───────────────────────────────────────────── */
  .apps-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 0.35rem;
  }

  /* ── App chip ───────────────────────────────────────────── */
  .app-chip {
    display: flex;
    align-items: center;
    gap: 0.45rem;
    padding: 0.5rem 0.65rem;
    border-radius: 6px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.75rem;
    font-weight: 500;
    text-decoration: none;
    color: var(--color-terminal-code-text);
    border: 1px solid transparent;
    transition: color 0.15s ease, background 0.15s ease, border-color 0.15s ease;
  }
  .app-chip:hover {
    color: var(--chip-color);
    background: color-mix(in srgb, var(--chip-color) 8%, transparent);
    border-color: color-mix(in srgb, var(--chip-color) 20%, transparent);
  }
  .app-chip-dot {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: var(--chip-color);
    flex-shrink: 0;
    opacity: 0.7;
    transition: opacity 0.15s ease;
  }
  .app-chip:hover .app-chip-dot {
    opacity: 1;
  }

  /* ── Buy button ─────────────────────────────────────────── */
  .nav-buy {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.8rem;
    font-weight: 600;
    padding: 0.5rem 1rem;
    background: var(--color-terminal-green);
    color: var(--color-terminal-bg);
    border-radius: 4px;
    text-decoration: none;
    transition: background 0.15s ease, transform 0.15s ease;
    white-space: nowrap;
    flex-shrink: 0;
  }
  .nav-buy:hover {
    background: var(--color-terminal-green-hover);
    transform: translateY(-1px);
  }

  /* ── Mobile menu trigger (hamburger) ───────────────────────── */
  .mobile-menu-trigger {
    display: none;
    width: 40px;
    height: 40px;
    align-items: center;
    justify-content: center;
    background: transparent;
    border: 1px solid rgba(var(--color-terminal-border-rgb),0.9);
    border-radius: 8px;
    color: var(--color-terminal-code-text);
    cursor: pointer;
    flex-shrink: 0;
    transition: color 0.15s ease, background 0.15s ease, border-color 0.15s ease;
  }
  .mobile-menu-trigger:hover,
  .mobile-menu-trigger[aria-expanded="true"] {
    color: var(--color-terminal-green);
    background: rgba(var(--color-terminal-green-rgb),0.07);
    border-color: rgba(var(--color-terminal-green-rgb),0.25);
  }
  .burger-top, .burger-mid, .burger-bot {
    transition: transform 0.2s ease, opacity 0.2s ease;
    transform-origin: center;
  }
  .mobile-menu-trigger[aria-expanded="true"] .burger-top { transform: translateY(4px) rotate(45deg); }
  .mobile-menu-trigger[aria-expanded="true"] .burger-mid { opacity: 0; }
  .mobile-menu-trigger[aria-expanded="true"] .burger-bot { transform: translateY(-4px) rotate(-45deg); }

  /* ── Mobile menu panel ──────────────────────────────────────── */
  .mobile-menu-panel {
    position: absolute;
    top: calc(100% + 0.5rem);
    left: 0;
    right: 0;
    background: rgba(var(--color-terminal-bg-rgb),0.97);
    border: 1px solid rgba(var(--color-terminal-border-rgb),0.9);
    border-radius: 10px;
    padding: 0.75rem;
    box-shadow: 0 20px 60px rgba(0,0,0,0.6), 0 0 0 1px rgba(var(--color-hover-tint),0.03);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
    opacity: 0;
    visibility: hidden;
    transform: translateY(-6px);
    transition: opacity 0.18s ease, transform 0.18s ease, visibility 0.18s;
    z-index: 100;
    max-height: calc(100vh - 5rem);
    overflow-y: auto;
  }
  .mobile-menu-panel.open {
    opacity: 1;
    visibility: visible;
    transform: translateY(0);
  }
  .mobile-menu-link {
    display: block;
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.9rem;
    padding: 0.7rem 0.6rem;
    border-radius: 6px;
    text-decoration: none;
    color: var(--color-terminal-code-text);
    transition: color 0.15s ease, background 0.15s ease;
  }
  .mobile-menu-link:hover,
  .mobile-menu-link--active {
    color: var(--color-terminal-green);
    background: rgba(var(--color-terminal-green-rgb),0.07);
  }
  .mobile-menu-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.68rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--color-terminal-muted);
    margin: 0.75rem 0 0.5rem 0.6rem;
  }
  .mobile-apps-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 0.35rem;
  }

  /* ── Mobile ─────────────────────────────────────────────── */
  @media (max-width: 768px) {
    .nav-center { display: none !important; }
    .mobile-menu-trigger { display: flex; }
  }
  @media (max-width: 420px) {
    .mobile-apps-grid { grid-template-columns: 1fr; }
  }
</style>

<script>
  function initNav() {
    // ── Scroll blur ────────────────────────────────────────
    const nav = document.getElementById('main-nav');
    if (nav) {
      const onScroll = () => nav.classList.toggle('scrolled', window.scrollY > 20);
      window.removeEventListener('scroll', (window as any).__navScroll);
      (window as any).__navScroll = onScroll;
      window.addEventListener('scroll', onScroll, { passive: true });
      onScroll();
    }

    // ── Apps dropdown ──────────────────────────────────────
    const trigger = document.getElementById('apps-trigger');
    const panel   = document.getElementById('apps-panel');
    const wrapper = document.getElementById('apps-dropdown');
    if (!trigger || !panel || !wrapper) return;

    // Remove old listeners by cloning nodes
    const newTrigger = trigger.cloneNode(true) as HTMLElement;
    const newWrapper = wrapper.cloneNode(false) as HTMLElement;
    // Re-attach panel and trigger into wrapper clone then swap
    // Simpler: just track with an abort controller
    const ac = new AbortController();
    const { signal } = ac;
    // Clean up previous controller if any
    const prev = (wrapper as any).__navAc as AbortController | undefined;
    if (prev) prev.abort();
    (wrapper as any).__navAc = ac;

    let closeTimer: ReturnType<typeof setTimeout> | null = null;

    const open = () => {
      if (closeTimer) { clearTimeout(closeTimer); closeTimer = null; }
      panel.classList.add('open');
      trigger.setAttribute('aria-expanded', 'true');
    };
    const scheduleClose = () => {
      closeTimer = setTimeout(() => {
        panel.classList.remove('open');
        trigger.setAttribute('aria-expanded', 'false');
      }, 140);
    };
    const cancelClose = () => {
      if (closeTimer) { clearTimeout(closeTimer); closeTimer = null; }
    };
    const closeNow = () => {
      if (closeTimer) { clearTimeout(closeTimer); closeTimer = null; }
      panel.classList.remove('open');
      trigger.setAttribute('aria-expanded', 'false');
    };

    wrapper.addEventListener('mouseenter', open,          { signal });
    wrapper.addEventListener('mouseleave', scheduleClose, { signal });
    panel.addEventListener('mouseenter',   cancelClose,   { signal });
    panel.addEventListener('mouseleave',   scheduleClose, { signal });

    trigger.addEventListener('click', (e) => {
      e.stopPropagation();
      panel.classList.contains('open') ? closeNow() : open();
    }, { signal });

    document.addEventListener('click', (e) => {
      if (!wrapper.contains(e.target as Node)) closeNow();
    }, { signal });

    document.addEventListener('keydown', (e) => {
      if (e.key === 'Escape') closeNow();
    }, { signal });

    // ── Mobile menu ─────────────────────────────────────────
    const mTrigger = document.getElementById('mobile-menu-trigger');
    const mPanel   = document.getElementById('mobile-menu-panel');
    if (!mTrigger || !mPanel) return;

    const mOpen = () => {
      mPanel.classList.add('open');
      mTrigger.setAttribute('aria-expanded', 'true');
    };
    const mClose = () => {
      mPanel.classList.remove('open');
      mTrigger.setAttribute('aria-expanded', 'false');
    };

    mTrigger.addEventListener('click', (e) => {
      e.stopPropagation();
      mPanel.classList.contains('open') ? mClose() : mOpen();
    }, { signal });

    mPanel.addEventListener('click', (e) => {
      if ((e.target as HTMLElement).closest('a')) mClose();
    }, { signal });

    document.addEventListener('click', (e) => {
      if (!mPanel.contains(e.target as Node) && !mTrigger.contains(e.target as Node)) mClose();
    }, { signal });

    document.addEventListener('keydown', (e) => {
      if (e.key === 'Escape') mClose();
    }, { signal });

    window.addEventListener('resize', () => {
      if (window.innerWidth > 768) mClose();
    }, { signal });
  }

  // Run on first load and after every View Transition navigation
  initNav();
  document.addEventListener('astro:page-load', initNav);
</script>
