# macOS
.DS_Store

# Editor
.idea/
.vscode/
*.swp
# local-only
.claude/
# local worktree checkout of the deploy/landing branch (see git-deploy.md)
/.worktree-landing/
# separate repo (github.com/aeon022/postctl), not a submodule
postctl/
